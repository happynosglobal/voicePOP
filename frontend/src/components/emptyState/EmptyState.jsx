const EmptyState = ({ text }) => {
  return (
    <div className="w-full mt-2 flex items-center justify-center text-center min-h-40 text-gray-500 rounded-md ">
      {text}
    </div>
  );
};

export default EmptyState;
